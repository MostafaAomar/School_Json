Using it as a folder reference (outside the application)

This code tells you what names to use for folders in your GitHub repository for the getUnitUrl function to work correctly.

Main folder: data

Subfolders (Materials): These should open folders with the ID names provided in the code (e.g., islamic, social, art).

Grade folders: Within each subject, open folders such as grade1, grade2.